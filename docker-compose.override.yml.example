# Developer overrides — copy this to docker-compose.override.yml and
# uncomment the sibling packages you want to debug locally.
#
# This file is auto-merged by `docker compose up` (no -f flags needed)
# when using the default docker-compose.yml (standalone mode).
#
# For custom dev setups (docker-compose.dev.yml), include explicitly:
#   docker compose -f docker-compose.dev.yml -f docker-compose.override.yml up --build

services:
    arches-rascolls:
        volumes:
            # The pre_dev.sh hook will auto-detect and pip install -e any
            # that are mounted.
            - ../arches-querysets:/opt/arches-querysets
            - ../arches-modular-reports:/opt/arches-modular-reports
            - ../arches-controlled-lists:/opt/arches-controlled-lists
            - ../arches-component-lab:/opt/arches-component-lab
            - ../arches-search:/opt/arches-search
