# Path to arches-docker infrastructure repo.
# Contains shared Dockerfiles, entrypoint.sh, nginx/supervisor configs.
# Default assumes arches-docker is a sibling directory.
ARCHES_DOCKER=../arches-docker

# Docker Compose settings
HOST_PROJECT_PORT=8060
HOST_PROJECT_DEBUG_PORT=5692
DOCKER_NETWORK=arches7-0-dev
ARCHES_CORE_HOST_DIR=../arches
